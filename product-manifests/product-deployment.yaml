apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-db-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        envFrom:
          - configMapRef:
              name: product-config
          - secretRef:
              name: product-secrets
        env:
          - name: MYSQL_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: MYSQL_ROOT_PASSWORD
          - name: MYSQL_USER
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: MYSQL_USER
          - name: MYSQL_PASSWORD
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: MYSQL_PASSWORD
          - name: MYSQL_DATABASE
            valueFrom:
              configMapKeyRef:
                name: product-config
                key: PRODUCT_DB


        ports:
        - containerPort: 3306
        volumeMounts:
        - name: mysql-data
          mountPath: /var/lib/mysql
      volumes:
      - name: mysql-data
        persistentVolumeClaim:
          claimName: data-pvc-product
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-deployment
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product
  template:
    metadata:
      labels:
        app: product
    spec:
      initContainers:
      - name: wait-for-mysql
        image: busybox:1.36
        command: ['sh', '-c', 'until nc -z product-db-service 3306; do echo "waiting for product-db..."; sleep 5; done;']

      containers:
      - name: product-service
        image: abdelkader97/product-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
        envFrom:
          - configMapRef:
              name: product-config
          - secretRef:
              name: product-secrets
        env:
          - name: DB_HOST
            value: product-db-service
          - name: DB_USER
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: MYSQL_USER
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: MYSQL_PASSWORD
          - name: DB_NAME 
            valueFrom:
              configMapKeyRef:
                name: product-config
                key: PRODUCT_DB
          - name: JWT_SECRET 
            valueFrom:
              secretKeyRef:
                name: product-secrets
                key: JWT_SECRET
          - name: JWT_EXPIRY 
            valueFrom:
              configMapKeyRef:
                name: auth-config
                key: JWT_EXPIRY
          - name: PORT
            value: "5000"
          - name: AUTH_SERVICE
            valueFrom:
              configMapKeyRef:
                name: product-config
                key: AUTH_SERVICE
          - name: PRODUCT_SERVICE
            valueFrom:
              configMapKeyRef:
                name: product-config
                key: PRODUCT_SERVICE
