_format_version: "3.0"
_transform: true

services:
  - name: auth-service
    url: http://auth-service:5000
    routes:
      - name: auth-routes
        paths:
          - /auth

  - name: users-service
    url: http://auth-service:5000
    routes:
      - name: users-routes
        paths:
          - /users

  - name: product-service
    url: http://product-service:5000
    routes:
      - name: product-routes
        paths:
          - /products
  

  